#' @param filter the 'filter' argument allows filtering of the point-cloud to work with points of
#' interest. For a given stage when a filter is applied, only the points that meet the criteria are processed.
#' The most common strings are `Classification == 2"`, `"Z > 2"`, `"Intensity < 100"`. For more details see
#' \link{filters}.
#' @md
